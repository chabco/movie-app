<% include head%>

<body>
    <div class="container">
        <div class="row">
            <div class="col col-sm8 offset-sm-2">
                <h1><%= parsedData.title %></h1>
                <!-- <img src="<%= parsedData.imageBaseUrl + parsedData.poster_path %>" /> -->
                <img src="<%= imageBaseUrl + parsedData.poster_path %>" />
                <p><strong>Overview: </strong><%= parsedData.overview %></p>
                <p><strong>Release Date: </strong> <%= parsedData.release_date %></p>

            </div>
        </div>
    </div>
</body>
</html>